/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ItemsPipe } from './items.pipe';

describe('Pipe: Items', () => {
  it('create an instance', () => {
    let pipe = new ItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
