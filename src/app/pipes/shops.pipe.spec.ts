/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShopsPipe} from './shops.pipe';

describe('Pipe: ShopsPipe', () => {
  it('create an instance', () => {
    let pipe = new ShopsPipe();
    expect(pipe).toBeTruthy();
  });
});
